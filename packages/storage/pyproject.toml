[project]
name = "cosmos-storage"
version = "0.1.0"
description = "Vector store and graph store implementations"
requires-python = ">=3.12"
dependencies = [
    "cosmos-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cosmos_storage"]

[tool.uv.sources]
cosmos-core = { workspace = true }
